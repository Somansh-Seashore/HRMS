{
  "name": "hrms-monorepo",
  "version": "1.0.0",
  "description": "HRMS Monorepo with Frontend and Backend",
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "install:frontend": "cd frontend && npm install",
    "install:all": "npm run install:frontend"
  },
  "dependencies": {
    "twilio": "^5.10.4"
  }
}
